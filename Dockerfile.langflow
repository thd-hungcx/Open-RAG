# syntax=docker/dockerfile:1.4
FROM langflowai/langflow:1.8.0

RUN pip install uv \
    && mkdir -p /app/langflow-data

COPY --chmod=755 entrypoint.langflow.sh /app/entrypoint.sh

EXPOSE 7860

CMD ["/app/entrypoint.sh"]
