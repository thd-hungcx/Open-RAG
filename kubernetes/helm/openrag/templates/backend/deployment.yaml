{{- if .Values.backend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openrag.fullname" . }}-backend
  namespace: {{ include "openrag.namespace" . }}
  labels:
    {{- include "openrag.backend.labels" . | nindent 4 }}
spec:
  replicas: 1  # Single pod for vertical scaling
  strategy:
    type: Recreate  # Required for RWO PVCs
  selector:
    matchLabels:
      {{- include "openrag.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openrag.backend.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/secret-opensearch: {{ include (print $.Template.BasePath "/secrets/opensearch-secret.yaml") . | sha256sum }}
        checksum/secret-langflow: {{ include (print $.Template.BasePath "/secrets/langflow-secret.yaml") . | sha256sum }}
        checksum/config-flows: {{ include (print $.Template.BasePath "/configmaps/flow-ids-configmap.yaml") . | sha256sum }}
        checksum/config-app: {{ include (print $.Template.BasePath "/configmaps/app-config-configmap.yaml") . | sha256sum }}
        checksum/secret-backend: {{ include (print $.Template.BasePath "/secrets/backend-secret.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "openrag.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- if and .Values.backend.persistence.documents.enabled .Values.backend.persistence.documents.loadDefaults .Values.backend.persistence.documents.git.enabled }}
        - name: load-default-documents
          image: curlimages/curl:8.5.0
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu

              DOCS_DIR="{{ .Values.backend.persistence.documents.mountPath }}"

              OWNER="{{ .Values.backend.persistence.documents.git.owner }}"
              REPO="{{ .Values.backend.persistence.documents.git.repo }}"
              BRANCH="{{ .Values.backend.persistence.documents.git.branch }}"
              {{- if .Values.backend.persistence.documents.git.hash }}
              GIT_REF="{{ .Values.backend.persistence.documents.git.hash }}"
              {{- else }}
              GIT_REF="${BRANCH}"
              {{- end }}
              REPO_PATH="{{ .Values.backend.persistence.documents.git.path }}"

              # Raw base URL: https://raw.githubusercontent.com/<owner>/<repo>/<ref>/<path>
              GITHUB_RAW_URL="https://raw.githubusercontent.com/${OWNER}/${REPO}/${GIT_REF}/${REPO_PATH}"

              mkdir -p "$DOCS_DIR"

              if [ -z "$(ls -A "$DOCS_DIR" 2>/dev/null)" ]; then
                echo "Loading default documents from ${OWNER}/${REPO}@${GIT_REF}/${REPO_PATH} ..."

                download_with_retry() {
                  file="$1"
                  url="${GITHUB_RAW_URL}/${file}"
                  dest="${DOCS_DIR}/${file}"
                  max_retries=3
                  timeout=60
                  retry=0

                  while [ "$retry" -lt "$max_retries" ]; do
                    echo "Downloading ${file} (attempt $((retry + 1))/${max_retries})..."
                    if curl -f -L -m "$timeout" -o "$dest" "$url"; then
                      echo "OK: ${file}"
                      return 0
                    fi
                    retry=$((retry + 1))
                    if [ "$retry" -lt "$max_retries" ]; then
                      echo "Retrying ${file}..."
                      sleep 2
                    fi
                  done

                  echo "ERROR: Failed to download ${file} from ${url}"
                  return 1
                }

                failed=0
{{- range .Values.backend.persistence.documents.git.files }}
                if ! download_with_retry "{{ . }}"; then failed=1; fi
{{- end }}

                if [ "$failed" -eq 1 ]; then
                  echo "ERROR: One or more documents failed to download"
                  exit 1
                fi

                echo "Documents loaded: $(ls "$DOCS_DIR")"
              else
                echo "Documents already exist in $DOCS_DIR, skipping download."
              fi
          volumeMounts:
            - name: documents
              mountPath: {{ .Values.backend.persistence.documents.mountPath }}
        {{- end }}
        {{- if .Values.langflow.enabled }}
        - name: wait-for-langflow
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for Langflow to be ready..."
              until nc -z {{ include "openrag.fullname" . }}-langflow {{ .Values.langflow.service.port }}; do
                echo "Langflow not ready, sleeping 5s..."
                sleep 5
              done
              echo "Langflow is ready!"
        {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            # OpenSearch connection (external SaaS)
            - name: OPENSEARCH_HOST
              value: {{ .Values.global.opensearch.host | quote }}
            - name: OPENSEARCH_PORT
              value: {{ .Values.global.opensearch.port | quote }}
            - name: OPENSEARCH_USERNAME
              value: {{ .Values.global.opensearch.username | quote }}
            {{- if .Values.global.opensearch.password }}
            - name: OPENSEARCH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-opensearch
                  key: password
            {{- end }}
            - name: OPENSEARCH_INDEX_NAME
              value: {{ .Values.global.opensearch.indexName | quote }}
            # Langflow connection
            - name: LANGFLOW_URL
              value: {{ include "openrag.langflow.url" . | quote }}
            {{- if .Values.backend.langflowPublicUrl }}
            - name: LANGFLOW_PUBLIC_URL
              value: {{ .Values.backend.langflowPublicUrl | quote }}
            {{- end }}
            - name: LANGFLOW_AUTO_LOGIN
              value: {{ .Values.langflow.auth.autoLogin | quote }}
            - name: LANGFLOW_SUPERUSER
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-langflow
                  key: superuser
            - name: LANGFLOW_SUPERUSER_PASSWORD
              valueFrom:
                  name: {{ include "openrag.fullname" . }}-langflow
                  key: superuser-password
            - name: LANGFLOW_KEY
              value: {{ .Values.langflow.auth.apiKey | default "" | quote }}
            - name: LANGFLOW_KEY_RETRIES
              value: {{ .Values.langflow.auth.keyRetries | default "15" | quote }}
            - name: LANGFLOW_KEY_RETRY_DELAY
              value: {{ .Values.langflow.auth.keyRetryDelay | default "2.0" | quote }}
            - name: LANGFLOW_VERSION
              value: {{ .Values.langflow.image.tag | default .Values.global.imageTag | quote }}
            - name: LOG_FORMAT
              value: {{ .Values.backend.logFormat | default "json" | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.backend.logLevel | default "INFO" | quote }}
            - name: SERVICE_NAME
              value: {{ .Values.backend.serviceName | default "openrag" | quote }}
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-backend
                  key: session-secret
            # Flow IDs from ConfigMap
            - name: LANGFLOW_CHAT_FLOW_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "openrag.fullname" . }}-flow-ids
                  key: chat-flow-id
            - name: LANGFLOW_INGEST_FLOW_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "openrag.fullname" . }}-flow-ids
                  key: ingest-flow-id
            - name: LANGFLOW_URL_INGEST_FLOW_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "openrag.fullname" . }}-flow-ids
                  key: url-ingest-flow-id
            - name: NUDGES_FLOW_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "openrag.fullname" . }}-flow-ids
                  key: nudges-flow-id
            # Feature flags
            - name: DISABLE_INGEST_WITH_LANGFLOW
              value: {{ .Values.backend.features.disableIngestWithLangflow | quote }}
            - name: INGEST_SAMPLE_DATA
              value: {{ .Values.backend.features.ingestSampleData | quote }}
            # Logging
            - name: LOG_LEVEL
              value: {{ .Values.backend.logLevel | quote }}
            - name: ACCESS_LOG
              value: {{ .Values.backend.accessLog | quote }}
            {{- if .Values.backend.noColor }}
            - name: NO_COLOR
              value: "1"
            {{- end }}
            # Timeouts
            - name: LANGFLOW_TIMEOUT
              value: {{ .Values.backend.langflowTimeout | quote }}
            - name: LANGFLOW_CONNECT_TIMEOUT
              value: {{ .Values.backend.langflowConnectTimeout | quote }}
            - name: INGESTION_TIMEOUT
              value: {{ .Values.backend.ingestionTimeout | quote }}
            # Concurrency
            - name: UPLOAD_BATCH_SIZE
              value: {{ .Values.backend.uploadBatchSize | quote }}
            {{- if .Values.backend.maxWorkers }}
            - name: MAX_WORKERS
              value: {{ .Values.backend.maxWorkers | quote }}
            {{- end }}
            # LLM Provider keys
            {{- if .Values.llmProviders.openai.enabled }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-llm-providers
                  key: openai-api-key
            {{- end }}
            {{- if .Values.llmProviders.anthropic.enabled }}
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-llm-providers
                  key: anthropic-api-key
            {{- end }}
            {{- if .Values.llmProviders.watsonx.enabled }}
            - name: WATSONX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-llm-providers
                  key: watsonx-api-key
            - name: WATSONX_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-llm-providers
                  key: watsonx-endpoint
            - name: WATSONX_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-llm-providers
                  key: watsonx-project-id
            {{- end }}
            # Docling service URL (for document processing in flows)
            - name: DOCLING_SERVE_URL
              value: {{ include "openrag.docling.url" . | quote }}
            {{- if .Values.llmProviders.ollama.enabled }}
            - name: OLLAMA_ENDPOINT
              value: {{ .Values.llmProviders.ollama.endpoint | quote }}
            {{- end }}
            # OAuth credentials
            {{- if .Values.global.oauth.google.enabled }}
            - name: GOOGLE_OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-oauth
                  key: google-client-id
            - name: GOOGLE_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-oauth
                  key: google-client-secret
            {{- end }}
            {{- if .Values.global.oauth.microsoft.enabled }}
            - name: MICROSOFT_GRAPH_OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-oauth
                  key: microsoft-client-id
            - name: MICROSOFT_GRAPH_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-oauth
                  key: microsoft-client-secret
            {{- end }}
            # AWS credentials
            {{- if .Values.backend.aws.enabled }}
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-aws
                  key: access-key-id
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "openrag.fullname" . }}-aws
                  key: secret-access-key
            {{- end }}
            # Webhook configuration
            {{- if .Values.backend.webhook.enabled }}
            - name: WEBHOOK_BASE_URL
              value: {{ .Values.backend.webhook.baseUrl | quote }}
            {{- end }}
          volumeMounts:
            {{- if .Values.backend.persistence.documents.enabled }}
            - name: documents
              mountPath: {{ .Values.backend.persistence.documents.mountPath }}
            {{- end }}
            {{- if .Values.backend.persistence.keys.enabled }}
            - name: keys
              mountPath: {{ .Values.backend.persistence.keys.mountPath }}
            {{- end }}
            {{- if .Values.backend.persistence.config.enabled }}
            - name: config
              mountPath: {{ .Values.backend.persistence.config.mountPath }}
            {{- end }}
            {{- if .Values.langflow.persistence.enabled }}
            - name: flows
              mountPath: /app/flows
              subPath: {{ .Values.langflow.persistence.flowsSubPath }}
              readOnly: true
            {{- end }}
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          {{- if .Values.backend.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.backend.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.backend.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.backend.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.backend.livenessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.backend.readinessProbe.enabled }}
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  # Check OpenSearch health
                  if ! curl -sf -k -u "${OPENSEARCH_USERNAME}:${OPENSEARCH_PASSWORD}" "{{ include "openrag.opensearch.url" . }}" > /dev/null 2>&1; then
                    echo "OpenSearch health check failed"
                    exit 1
                  fi
                  
                  # Check Langflow health
                  if ! curl -sf "{{ include "openrag.langflow.url" . }}/health" > /dev/null 2>&1; then
                    echo "Langflow health check failed"
                    exit 1
                  fi
                  
                  # Check Docling health
                  if ! curl -sf "{{ include "openrag.docling.url" . }}/health" > /dev/null 2>&1; then
                    echo "Docling health check failed"
                    exit 1
                  fi
                  
                  echo "All health checks passed"
                  exit 0
            initialDelaySeconds: {{ .Values.backend.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.backend.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.backend.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.backend.readinessProbe.failureThreshold }}
          {{- end }}
      volumes:
        {{- if .Values.backend.persistence.documents.enabled }}
        - name: documents
          emptyDir: {}
        {{- end }}
        {{- if .Values.backend.persistence.keys.enabled }}
        - name: keys
          persistentVolumeClaim:
            claimName: {{ include "openrag.fullname" . }}-keys
        {{- end }}
        {{- if .Values.backend.persistence.config.enabled }}
        - name: config
          persistentVolumeClaim:
            claimName: {{ include "openrag.fullname" . }}-config
        {{- end }}
        {{- if .Values.langflow.persistence.enabled }}
        - name: flows
          persistentVolumeClaim:
            claimName: {{ include "openrag.fullname" . }}-langflow
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
